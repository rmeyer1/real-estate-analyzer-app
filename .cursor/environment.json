{
  "install": "npm install && cd backend && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt",
  "terminals": [
    {
      "name": "Next.js Frontend",
      "command": "cd real-estate-analyzer-app && npm run dev"
    },
    {
      "name": "FastAPI Backend",
      "command": "cd real-estate-analyzer-app/backend && source venv/bin/activate && uvicorn main:app --reload"
    }
  ]
}