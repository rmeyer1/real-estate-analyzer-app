{
  "install": "npm install && cd backend && apt-get update && apt-get install -y python3-venv && python3 -m venv venv && . venv/bin/activate && pip install -r requirements.txt",
  "terminals": [
    {
      "name": "Next.js Frontend",
      "command": "cd real-estate-analyzer-app && npm run dev"
    },
    {
      "name": "FastAPI Backend",
      "command": "cd real-estate-analyzer-app/backend && . venv/bin/activate && uvicorn main:app --reload"
    }
  ]
}